// TODO Phase 5 — scatter: TWAS (bps) vs slippage (bps) — liquidity vs execution cost
import type { TCAResult } from "@/types";

interface SpreadScatterProps {
  results: TCAResult[];
}

export function SpreadScatter(_props: SpreadScatterProps) {
  return <div className="text-gray-400 text-sm">SpreadScatter — stub (Phase 5)</div>;
}
