# Bloomberg TCA Bridge dependencies
#
#   pip install -r requirements.txt
#
# On a corporate network you may need your firm's package index:
#   pip install -r requirements.txt --index-url https://your.mirror/simple

fastapi
uvicorn[standard]

# Bloomberg Python SDK. Optional: without it the bridge still starts and the
# app runs, but no market data is available.
blpapi
